import '@localTypes/global';

const {
  device: Device,
  bridge: Bridge,
  register: HippyRegister,
  on: HippyOn,
  document: UIManager,
  asyncStorage: AsyncStorage,
} = global.Hippy;

export {
  Device,
  Bridge,
  HippyRegister,
  HippyOn,
  UIManager,
  AsyncStorage,
};
