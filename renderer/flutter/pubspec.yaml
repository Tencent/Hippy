name: voltron_renderer
description: A flutter plugin project to render hippy page.
version: 0.1.0
homepage:

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.0.10
  ffi: ^1.0.0
  sprintf: ^6.0.0
  provider: ^5.0.0
  pull_to_refresh: ^2.0.0
  collection: ^1.15.0
  path: ^1.8.0
  utf: ^0.9.0+5
  fluttertoast: ^8.0.8
  gradient_like_css: ^0.5.1
  qr_flutter: ^4.0.0
  keyboard_utils: ^1.3.4
  webview_flutter: ^3.0.2
  waterfall_flow: ^3.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

dependency_overrides:
  win32: 2.6.1


